{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(forms) }}
    {{ form_widget(forms) }}
    <input type="submit" class="btn btn-primary" value="Enregistrer" />
    {{ form_end(forms) }}

{% endblock %}